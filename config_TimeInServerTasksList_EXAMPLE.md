# 📄 服务器在线时长任务添加示例（TimeInServer）

下面说明如何添加 **在服务器上停留指定时间** 的任务。  
详细参数说明请参阅主配置文件 **config_TimeInServerTasksList.json**。

---

## 🧩 额外参数说明（typeTask = TimeInServer）

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskParamNumber` | `float array` | 要求玩家在服务器停留的时间（单位：分钟）。用于在界面显示任务需求。 |

---

## 🔧 第一步：在 TASKS 文件夹添加主任务配置

在主任务列表中（TASKS 文件夹内文件）加入一个新的任务配置：

```json
{
        "taskID": 1501,
        "givePoints": 1000,
        "typeTask": "TimeInServer",
        "iconTask": "relife_SeasonPass/images/task_icons/servertime.edds",
        "titleTask": "时间是相对的",
        "descTask": "在服务器上停留 120 分钟。",
        "taskParamNumber": [
            120.0
        ],
        "taskParamText": [
        ]
}
```

---

## 🔧 第二步：在 config_TimeInServerTasksList.json 添加附加参数

该文件用于连接任务 ID，确保系统识别此任务属于 TimeInServer 类型。

```json
{
        "taskID": 1501
}
```

---

