# 📄 config_TimeInServerTasksList 配置说明

该配置用于为 **TimeInServer（服务器在线时长）** 类型任务添加额外参数。

此类任务用于：  
⭐ 玩家累计在服务器在线达到指定时间后任务完成  
⭐ 通常配合任务主配置中的 `taskParamNumber`（分钟数）使用  

---

## 🧩 参数说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskID` | `integer` | 此配置对应的任务 ID（来自 TASKS 文件夹） |

---

## 🧱 配置示例

> ❗ 该配置类型非常简单，仅需要指定任务 ID  
> 在线时间在主任务配置中通过 `taskParamNumber` 设置分钟数

```json
{
    "taskID": 501
}
```

---

