# 📄 制作任务添加示例（RecipeCraft）

下面说明如何为 **RecipeCraft（制作任务）** 类型添加任务。  
适用于通过 **DayZ 原版（vanilla）制作系统** 制作物品的情况。  
更多参数请查看附加配置：**config_RecipeTasksList.json**

---

## 🧩 typeTask = RecipeCraft 的额外参数说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskParamNumber` | `float array` | 需要执行该制作次数，达到此数量后任务完成。 |
| `taskParamText` | `string array` | 制作出的物品类名，用于显示在任务界面的物品预览区域（与 iconTask 图标同位置）。 |

---

## 🔧 第一步：在 TASKS 文件夹添加主任务

任务需要添加到 TASKS 文件夹中的任务配置文件中，例如：

```json
{
    "taskID": 43,
    "givePoints": 1000,
    "typeTask": "RecipeCraft",
    "iconTask": "relife_SeasonPass/images/reward_empty.edds",
    "titleTask": "绳子",
    "descTask": "用破布制作一条绳子。",
    "taskParamNumber": [
        1.0
    ],
    "taskParamText": [
        "Rope"
    ]
}
```

说明：  
- `taskParamText` 的 `"Rope"` 将在任务 UI 内显示为物品预览图标文字  
- `iconTask` 可使用占位图，也可更换自定义图标  

---

## 🔧 第二步：在 config_RecipeTasksList.json 添加附加参数

该配置用于告诉系统：  
此任务对应哪个 **制作配方（recipeName）**。

```json
{
    "taskID": 43,
    "recipeName": "CraftRagRope"
}
```

说明：  
- `recipeName` 必须是 DayZ 原版或模组中 **真实存在的配方类名**  
- `taskID` 必须与主任务完全一致  

---

## 🎥 如果你仍然不确定，可以观看详细教程（俄语）
https://youtu.be/N1IO-jqqAFg

---

