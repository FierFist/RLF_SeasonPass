# 📄 访问地点任务添加示例（LocationCheck）

下面说明如何为 **LocationCheck（地点访问任务）** 添加任务。  
详细参数请参阅附加配置文件：**config_LocationCheckTasksList.json**

---

## 🧩 typeTask = LocationCheck 的额外参数说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskParamNumber` | `float array` | 玩家需要访问的地点数量。用于任务界面显示。 |

---

## 🔧 第一步：在 TASKS 文件夹添加主任务

主任务写在 TASKS 文件夹的任务定义文件中，例如：

```json
{
    "taskID": 222,
    "givePoints": 1000,
    "typeTask": "LocationCheck",
    "iconTask": "relife_SeasonPass/images/reward_empty.edds",
    "titleTask": "地点探访",
    "descTask": "访问两个地点。",
    "taskParamNumber": [
        2.0
    ],
    "taskParamText": [
    ]
}
```

说明：  
- `taskParamNumber` = 2 → 玩家必须访问 2 个地点  
- 图标可使用占位图，也可自定义  

---

## 🔧 第二步：在 config_LocationCheckTasksList.json 添加附加参数

此处指定玩家必须访问的地点坐标与半径。

```json
{
    "taskID": 222,
    "locationLists":  [
        [13910.5, 5.169, 11821.8],
        [6129.59, 3.4455, 7272.06]
    ],
    "locationRadiuses":  [
        100,
        100
    ]
}
```

说明：  
- `locationLists`：需要访问的世界坐标（可填多个）  
- `locationRadiuses`：每个坐标的触发范围（单位：米），数量必须与坐标数量相同  

---

## 🎥 更详细的教程视频（俄语）
https://youtu.be/8HoqGqBXQEw

---

