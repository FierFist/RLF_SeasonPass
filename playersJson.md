# 📄 PLAYERS 配置说明（玩家数据）

此配置文件用于保存玩家在 Season Pass（季票系统）中的所有进度与状态。

每位玩家登录服务器时，系统会根据此文件判断：  
- 当前属于哪个赛季  
- 经验是否正确  
- 是否拥有 Premium  
- 已领取哪些奖励  
- 当前每日/每周任务是什么  
- 当前任务进度情况  

如果 `currentSeason` 不匹配服务器当前赛季，则玩家旧赛季数据会被自动清除，并重新生成属于当前赛季的数据。

---

## 🧩 参数说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `GUID` | `string` | 玩家唯一 GUID（DayZ 内部生成） |
| `Name` | `string` | 玩家名称 |
| `SteamID` | `string` | 玩家 Steam64 ID |
| `currentSeason` | `string` | 玩家当前所在赛季 ID<br>如果与服务器赛季不一致，数据将被重置 |
| `seasonPoints` | `int` | 玩家当前的赛季经验值（必须是整数） |
| `havePremium` | `bool` | 玩家是否已激活 Premium（1=是，0=否） |
| `changeDayAttempt` | `int` | 玩家当前的每日任务更换次数 |
| `changeWeekAttempt` | `int` | 玩家当前的每周任务更换次数 |
| `dayTaskLastDate` | `int array` | 最近一次刷新每日任务的日期（天、月、年） |
| `weekTaskLastDate` | `int array` | 最近一次刷新每周任务的日期（天、月、年） |
| `dayTasks` | `int array` | 当前玩家的每日任务 ID 列表（来自 config_TasksList.json） |
| `weekTasks` | `int array` | 当前玩家的每周任务 ID 列表 |
| `seasonPass` | `array` | 任务进度状态列表，每个元素包含：taskID, taskProgress, taskType, isDayTask |
| `claimedReward` | `string array` | 已领取奖励列表，格式为：`等级_是否免费`（1=免费，0=付费） |

---

## 📁 seasonPass 内部结构说明

每个元素通常长这样：

| 字段 | 类型 | 说明 |
|------|------|------|
| `taskID` | `int` | 任务 ID |
| `taskProgress` | `string` | 当前任务进度值（字符串存储） |
| `taskType` | `string` | 任务类型（EntityKill、WalkDistance、CollectItems 等） |
| `isDayTask` | `int` | 是否为每日任务（1=是，0=否） |

---

## 🧱 示例配置（完整）

```json
{
    "GUID": "1PW_zxejn11XvJ03h5SFi45AOVci-oBLJMLydBnjFR4=",
    "Name": "VirusomanVS",
    "SteamID": "76561199479047427",
    "currentSeason": "April_228",
    "seasonPoints": 2280,
    "havePremium": 1,
    "dayTaskLastDate": [
        6,
        4,
        2025
    ],
    "weekTaskLastDate": [
        5,
        4,
        2025
    ],
    "dayTasks": [
        2,
        4,
        6,
        3
    ],
    "weekTasks": [
        10,
        13,
        11
    ],
    "seasonPass": [
        {
            "taskID": 111,
            "taskProgress": "1",
            "taskType": "EntityKill",
            "isDayTask": 1
        }
    ],
    "claimedReward": [
        "1_1",
        "2_1",
        "1_0"
    ]
}
```

---

